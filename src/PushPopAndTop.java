/*
There is a stack of integers which is currently empty. You are given
 an integer n and there are n operations that you need to perform on the stack.

The next n line contains one of the following 3 operations:

1 x : Push x to the top of the stack.

2 : Pop an element from the top of the stack. If the stack is empty, do nothing.


 */
import java.util.*;

public class PushPopAndTop {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int a,b;
        int n=sc.nextInt();
        for (int i=0;i<n;i++){
            a=sc.nextInt();
            switch (a){
                case 1:b=sc.nextInt();
                break;
                case 2:
            }
        }
    }
}
